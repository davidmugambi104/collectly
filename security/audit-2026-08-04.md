# Collectly Security & Infra Audit — 2026-08-04

**Run by:** `collectly-secret-rotation-auditor` + `collectly-dependency-vulnerability-scanner`
**Repo:** `/home/user/.openclaw/workspace/collectly`

## Secret Hygiene

### Findings

- **Only `.env.example` is tracked.** Real `.env` is not committed — good.
- **CI workflow uses placeholder test secrets** (`sk_test_ci_stripe`, `test_ci`, etc.). These appear to be fake CI-only values, but should be confirmed.
- **Two operational docs contain secret-shaped placeholder patterns:**
  - `ops/setup-keys.md` — `sk_live_xxxxx`, `whsec_xxxxx`, `ca_xxxxx`, `TWILIO_AUTH_TOKEN: xxxxx`
  - `ops/infrastructure-unblock-brief.md` — commands writing `GMAIL_APP_PASSWORD`, `TWILIO_AUTH_TOKEN`, `TWILIO_ACCOUNT_SID` to `/home/davie/.openclaw/secrets/collectly/`
- **Git history cleanup occurred on 2026-07-31** (`ea9eafa`): `.audit/` folder untracked and secret-shaped placeholders scrubbed. This suggests a prior leak incident was already handled, but we should confirm no real secret remains in git history.
- **No real secret scanner installed** (`trufflehog`/`git-secrets`). Recommended: install one and scan full git history.

### QBO Client Secret Risk

- The repo correctly reads `QBO_CLIENT_SECRET` from `process.env`.
- **Action required:** Davie must log into the QBO developer console and rotate the secret if there is any chance the real value was exposed outside this environment (e.g., in the `.audit/` incident, chat logs, or screenshots).

### Recommended actions

1. Install `trufflehog` or `git-secrets` and run a full historical scan.
2. Confirm the `ops/setup-keys.md` and `ops/infrastructure-unblock-brief.md` placeholders are not real values.
3. Rotate QBO client secret via QBO developer console if not already done post-incident.
4. Move all local secrets under one path and ensure that path is in `.gitignore`.
5. Review CI workflow secrets to confirm they are non-production test values.

## Dependency Vulnerabilities

**Summary:** 48 vulnerabilities — 6 low, 18 moderate, 23 high, 1 critical.

### Critical

- `node-tar` — Uncontrolled recursion DoS via crafted long-path tar.
  - Fix: `npm audit fix` (patch available)

### High-severity groups

- `undici` (via `vercel` and Vercel subpackages) — multiple HTTP smuggling, CRLF injection, desync, and DoS issues.
  - Fix: `npm audit fix --force` will install `vercel@54.17.3` (breaking change — test before prod).
- `path-to-regexp` (via `@vercel/backends`) — regex DoS.
- `@vercel/backends` / `@vercel/build-utils` — high severity via transitive deps.
- `drizzle-kit` / `@esbuild-kit/*` — esbuild moderate-severity chain.
- `ai` / `@ai-sdk/*` — low-severity resource consumption.

### Recommended actions

1. Run `npm audit fix` to patch non-breaking fixes (e.g., `node-tar`).
2. For `vercel`/`undici` breaking update, create a preview branch, run `npm audit fix --force`, run tests, and deploy to preview before prod.
3. Set up weekly `npm audit` runs via `collectly-dependency-vulnerability-scanner`.

## Dev/Prod Isolation

- `.env.example` shows both `sandbox` and production-oriented variables mixed in one file.
- No separate `.env.development` / `.env.production` files observed.
- Secrets are stored locally in `/home/davie/.openclaw/secrets/collectly/` per operational docs, but that directory is not accessible in this run environment.
- **Recommended:** create explicit environment separation:
  - `.env.development`
  - `.env.production`
  - `.env.test` for CI
  - Vercel/ hosting provider env per target
  - Document which secrets belong to which environment in a new SOP.

## Next Steps

- [ ] Davie: confirm QBO secret rotation status and rotate if needed
- [ ] Davie: confirm CI workflow secrets are fake test values
- [ ] OpenClaw: install secret scanner and run historical scan
- [ ] OpenClaw: patch critical `node-tar` vulnerability
- [ ] OpenClaw: plan `vercel`/`undici` major-version update on preview branch
- [ ] OpenClaw: create dev/prod environment isolation SOP
